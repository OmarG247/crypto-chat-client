import React from "react";
import { Button, Text, StyleSheet, View } from "react-native";

const Home = () => {
  return (
    <View
      style={{
        height: "100%",
      }}
    >
      <Text>This is home</Text>
    </View>
  );
};

export default Home;
