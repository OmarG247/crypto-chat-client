import { StyleSheet, View } from "react-native";

const Header = ({
  back = false,
  options = false,
  handleOptions = null,
  handleBack = null,
}) => {
  return <View></View>;
};

const HeaderStyles = StyleSheet.create({
  container: {},
});

export default Header;
