import { StyleSheet } from "react-native";

export const containers = StyleSheet.create({
  basic: {
    width: "100%",
    display: "flex",
    flexDirection: "column",
    paddingVertical: 16,
  },
});
