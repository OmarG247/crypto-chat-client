import { StyleSheet } from "react-native";

export const effects = StyleSheet.create({
  glow: {
    shadowColor: "#FFFFFF",
    shadowOffset: {
      height: 0,
      width: 0,
    },
    shadowOpacity: 0.25,
    shadowRadius: 12,
  },
});
